<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PortableApp;assembly=PortableApp"
             x:Class="PortableApp.WetlandPlantsPage"
             Title="Wetland Plants">
  <ContentPage.Content>
    <StackLayout>
      <SearchBar x:Name="search" Placeholder="Search" />
               <!--SearchCommandParameter="{Binding Text, Source={x:Reference search}}"-->  
      <ListView x:Name="plantsList" ItemSelected="OnItemSelected" RowHeight="70" >
        <ListView.ItemTemplate>
          <DataTemplate>
            <ImageCell ImageSource="{local:ImageResource PortableApp.Resources.Images.cover-thumb.jpg}" Text="{Binding CommonName}" Detail="Detail text" />   
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>